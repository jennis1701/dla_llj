streamlit==1.28.0
# TensorFlow - cannot install on Python 3.14 yet. 
# Only needed for local training (python training/train.py)
# Not required for the Streamlit app which loads pre-trained models
numpy>=1.24.0
Pillow>=10.0.0
scikit-learn>=1.3.0
matplotlib>=3.8.0
seaborn>=0.12.0
pyyaml>=6.0